#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    char command[50];
    strcpy( command, "clear");
    system(command);
    printf("Fuck SP\n");
    return 0;


}




/* Parse the function and return each 'part'' */

/* Recognize with derivation rule must be applied and apply it (this means writing a funciton for   quotint rule, power rule, etc.) */

/* Return parent and differentiated function */
